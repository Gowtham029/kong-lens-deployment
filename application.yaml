apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kong-lens-app-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/gboie/argocd-kong-lens-app-config.git
    targetRevision: HEAD
    path: kong-lens-app
  destination: 
    server: https://kubernetes.default.svc
    namespace: kong-lens-app

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true